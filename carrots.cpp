#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, p;

  while (cin >> n >> p) {
    cin.ignore();

    string s;

    for (int i = 0; i < n; i++) {
      getline(cin, s);
    }

    cout << p << endl;

    break;
  }

  return 0;
}