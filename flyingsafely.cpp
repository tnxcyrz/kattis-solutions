#include <bits/stdc++.h>

using namespace std;

int main() {
  int test;
  cin >> test;

  while (test--) {
    int n, m;
    cin >> n >> m;

    while (m--) {
      int x, y;
      cin >> x >> y;
    }

    int ans = n - 1;
    cout << ans << endl;
  }

  return 0;
}